{
  "require": {
    "php": "^7.4",
    "cjdevstudios/array-mapper": "dev-main"
  },
  "repositories": [
    {
      "type": "vcs",
      "url": "https://github.com/CJDevStudios/array-mapper.git"
    }
  ],
  "require-dev": {
    "glpi-project/tools": "^0.1",
    "justinrainbow/json-schema": "^5.2"
  },
  "config": {
    "optimize-autoloader": true,
    "platform": {
      "php": "7.4.0"
    },
    "sort-packages": true
  },
  "scripts": {
    "test": "../../vendor/bin/atoum --force-terminal --ncc --mcn 1 --bf tests/bootstrap.php -d tests"
  }
}
